cmake_minimum_required(VERSION 2.8)

add_subdirectory(gtest)
set(GTEST_INCLUDE_DIRS ${GTEST_INCLUDE_DIRS} PARENT_SCOPE)
set(GTEST_LIBS_DIR ${GTEST_LIBS_DIR} PARENT_SCOPE)

if(NOT TESTS)
  add_subdirectory(nana)
  set(NANA_INCLUDE_DIRS ${NANA_INCLUDE_DIRS} PARENT_SCOPE)
  set(NANA_LIBS_DIR ${NANA_LIBS_DIR} PARENT_SCOPE)

  add_subdirectory(hqx)
  set(HQX_INCLUDE_DIRS ${HQX_INCLUDE_DIRS} PARENT_SCOPE)
  set(HQX_LIBS_DIR ${HQX_LIBS_DIR} PARENT_SCOPE)
endif()
