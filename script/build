#!/bin/sh

if [ ! -d build ]; then
  mkdir build
fi

cd build || exit 1

if [ "${1}" = "Debug" ]; then
  cmake .. -DCMAKE_BUILD_TYPE=Debug -DCMAKE_CXX_COMPILER="${COMPILER:-clang++}"
else
  cmake .. -DCMAKE_CXX_COMPILER="${COMPILER:-clang++}"
fi
make
