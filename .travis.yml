sudo: false

language: generic

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
      - llvm-toolchain-precise-3.8
    packages:
      - clang-3.8

env:
  - CC=clang-3.8 CXX=clang++-3.8

script:
  - ./script/build tests &&
    ./script/run_tests
